function escrever(aff){
	var soma = parseInt(valorA) + parseInt(valorC);
	info="Soma de A+C: "+soma+"\n";
	aff.value+=info;
}

function escrever2(aff){
	var mult = (valorB)*(valorD);
	info="Multiplicação de B*D: "+mult+"\n";
	aff.value+=info;	
}
